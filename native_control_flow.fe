fn main() {
    let mut count = 0;
    
    while (count < 5) {
        print(count);
        count = count + 1;
    }
    
    if (count == 5) {
        print(100);
    } else {
        print(200);
    }
}
