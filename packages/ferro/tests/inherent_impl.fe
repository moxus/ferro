// Test inherent impl blocks

struct Point {
    x: int,
    y: int,
}

impl Point {
    fn new(x: int, y: int) -> Point {
        Point { x: x, y: y }
    }

    fn sum(self: Point) -> int {
        self.x + self.y
    }
}

let p = Point::new(3, 4);
print(p.x);
print(p.y);
